{
  "kind": "build_request",
  "title": "Prepare production publish and enlarge header/logo",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Increase the visual size of the SiteHeader and its brand logo while preserving the current responsive behavior (mobile vs. md+) and overall layout (logo + brand text + nav + CTA + mobile menu).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "Increase the size of the header and logo"
        ]
      },
      "acceptanceCriteria": [
        "On mobile, the header bar is visibly taller than the current h-20 implementation and does not clip content.",
        "On md+ breakpoints, the header bar is visibly taller than the current md:h-24 implementation and does not clip content.",
        "The header logo image renders larger than the current h-[44px] (mobile) and md:h-[53px] (md+) values, without changing the underlying image file path.",
        "Desktop navigation and CTA remain aligned and do not wrap/collide with the logo/brand text at typical viewport widths (e.g., 1024px+).",
        "Mobile menu open/close behavior and smooth-scroll behavior continue to work correctly after the header size change (scroll offset accounts for the new header height)."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Add a production publish readiness flow: ensure there is a documented, repeatable preflight + deploy procedure and that the preflight checks (TypeScript compile + required static asset verification) can be run successfully before publishing.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-20"
        ],
        "quotes": [
          "Publish it"
        ]
      },
      "acceptanceCriteria": [
        "A developer can follow frontend/DEPLOYMENT.md to run production preflight checks and perform a production deployment to the Internet Computer.",
        "Running the production preflight runner fails with a non-zero exit code if TypeScript compilation fails or required static assets are missing, and succeeds (exit code 0) when all checks pass.",
        "DEPLOYMENT.md includes a post-deployment verification checklist that covers at least: header rendering across breakpoints, hero background rendering, navigation scroll offset correctness, and no missing asset 404s in production."
      ]
    }
  ],
  "constraints": [
    "Do not edit any files under frontend/src/components/ui or any paths listed as immutable in SYSTEM_CONTEXT.",
    "Keep all backend logic within the single Motoko actor in backend/main.mo (no additional backend services).",
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Adding new product features beyond enlarging the header/logo and preparing the app for production publishing.",
    "Changing the logo image file itself or generating a new logo image unless explicitly requested.",
    "Implementing third-party authentication providers or external backend/database integrations."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}